let avg={
    arr: [1, 2, 3, 4],
    avgfind:()=>{
         let sum = average.arr.reduce((a, b) => a + b, 0);
        console.log(sum / average.arr.length);
    }
}


const iseven=(a)=>((a%2)==0)
console.log(iseven(4))

